/// https://uva.onlinejudge.org/index.php?option=com_onlinejudge&Itemid=8&category=15&page=show_problem&problem=1246

#include <iostream>
#include <vector>
#include <vector>
#include <queue>
#include <set>
#include <stack>
#include <string.h>

using namespace std;

int main(){

	int N, M;
    int J,K;

    while(true)
    {
        vector<int> v;
        cin >> N;
        cin >> M;
        if (N == 0) break;

        for(int i = 0; i < M; i++)
        {
            cin >> J;
            cin >> K;
        }
        cout << N << endl;
    }

	return 0;
}